# Vote storage (optionnel). Sans ces variables, le vote fonctionne en mode local
# (chaque personne voit son propre vote, pas de total partage).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
